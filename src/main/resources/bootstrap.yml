server:
  port: 8761
  tomcat:
    max-threads: 128 
    min-spare-threads: 64
spring:
  application:
    name: eureka-center
    
    
#Eureka只作为Server端，所以不用向自身注册
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
